[alias]
b = "build"
c = "clippy --tests"
d = "doc"
t = "test"
r = "run --package basic"
rr = "run --release --package basic"
# space_example = ["run", "--release", "--", "\"command list\""]

[build]
rustdoc = "rustdoc"
rustdocflags = ["--default-theme=ayu"]

